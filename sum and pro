#include <stdio.h>

int main() {
    int n,r,sum=0,pro=1;
    printf( "enter n value\n");
    scanf( "%d",&n);
    while(n!=0)
    {
        r=n%10;
        pro=pro*r;
        sum=sum+r;
        n=n/10;
    }
    printf( "%d",pro-sum);

    return 0;
}
